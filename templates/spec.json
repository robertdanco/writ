{
  "project": "",
  "version": "1.0.0",
  "created_at": "",
  "description": "",
  "features": [
    {
      "id": "example-feature",
      "title": "Example Feature",
      "description": "Description of what this feature does from the user's perspective. Replace with real features via /sdd-ingest.",
      "depends_on": [],
      "priority": 1,
      "status": "pending",
      "acceptance_criteria": [
        {
          "description": "The main entry point exists",
          "type": "file_exists",
          "target": "src/example.js",
          "expected": ""
        },
        {
          "description": "Running the example produces expected output",
          "type": "command_succeeds",
          "target": "node src/example.js | grep -q 'Hello'",
          "expected": ""
        },
        {
          "description": "Tests pass for this feature",
          "type": "test_passes",
          "target": "npm test -- --grep 'example feature'",
          "expected": ""
        }
      ]
    }
  ]
}
